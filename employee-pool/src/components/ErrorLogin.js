const ErrorLogin = () => {

    return (
        <h3 className="error-login" data-testid="error-msg-login" >
            Error in the login. Verify username and password.
        </h3>
    );
}

export default ErrorLogin;
